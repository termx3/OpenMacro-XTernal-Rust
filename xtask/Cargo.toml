[package]
name = "xtask"
description = "Build / package / release automation for the XTernal workspace (cargo-xtask pattern)."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# anyhow = { workspace = true }
